# Binaries
/main
*.o

# Data files
*.csv
*.edf

# Patient data which should stay private and not be committed to the repository
patient.cfg

# Directories
.venv/
build/
*.bin
!biometric_filtered.bin
*.html
*.pyc
*.zarr/
src_web/node_modules/

# Viewer entry HTML is a tracked source file despite the blanket *.html ignore above
!src_web/index.html

# Quartz wiki build artifacts (web/build.sh) — never committed
/.quartz/
/public/
src_python/netlify.json
src_python/patient.json
src_python/biometric_filtered.json
biometric_filtered_meta.json
biometric_meta.json
.vscode/c_cpp_properties.json
src_web/dist/chart.js
Dmitry Ignatyev PSG data.pdf
.github/workflows/clean-machine-test.yml
ESP32-C6-heart-idf/reference/ESP32-C6-LCD-1.47-Test/
src_rust/polysom_rtdi/target

# Real Wi-Fi credentials for the ESP32 firmware — never committed.
# sdkconfig is a generated file that ends up containing merged secrets
# once sdkconfig.local is applied, so it's untracked too.
ESP32-C6-heart-idf/sdkconfig
ESP32-C6-heart-idf/sdkconfig.local