1. Run `src_python\install.bat` (or an equivalent for Linux)
2. Run command `python .\src_python\read_log.py -f .\biometric_filtered.bin -c --threshold 3 --skip 1500 --ignore_last 2500`
This generate the output .html, and start the web server and browser to show the chart